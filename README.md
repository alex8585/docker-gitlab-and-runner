# docker-gitlab-and-runner
- 1). It install Gitlab server and docker gitlab-runner both in docker containers.
- 2). There is  example of .gitlab-ci.yml in ci_cd folder
## install
- 1). docker-compose up 
